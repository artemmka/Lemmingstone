# Описание API
Здесь описано всё АПИ, используемое в приложении, с описанием структур данных


**Содержание**
1. Общее
    * 1.1. Адрес сервера
    * 1.2. Используемый протокол
2. Структуры данных
    * 2.1. Общий формат ответа
    * 2.2. Пользователь
    * 2.3. Сообщение
3. Список запросов
    * 3.1. Общие ошибки
4. Подробно
    * 4.1. login
    * 4.2. logout
    * 4.3. registration
    * 4.4. sendMessage
    * 4.5. getMessages
    * 4.6. getSpline
    * 4.7. generateRandomPoints
    * 4.8. generateMap
    * 4.9. changeName
    * 5.0. changePassword
    * 5.1. getCatalog
    * 5.2. respawnObjects
    * 5.3. givePosition
    * 5.4. getPosition
    * 5.5. getScores
    * 5.6 changeClass
    * 5.7 сhangeItem
    * 5.8 telepotLemming
    * 5.9 adminLogoutUser
    * 5.10 buyItem


## 1. Общее
### 1.1. Адрес сервера
`http://lemmingstone.local/api`

### 1.2. Используемый протокол
API полностью реализовано на http(s). 

Формат возвращаемых значений `JSON`.

Все методы, если это особо не оговорено, имеют тип `GET`.


## 2. Структуры данных
### 2.1. Общий формат ответа
`T` - какие-то данные. В случае успешного ответа возвращается `result = 'ok'` и поле `data` с данными.

В случае ошибки возвращается `result = 'error'` и поле `error` с кодом и текстом ошибки
```
Answer<T>: {
    result: 'ok' | 'error';
    data?: T;
    error?: {
        code: number;
        text: string;
    };
}
```

### 2.2. Пользователь
```
User: {
    id: number;
    token: string;
    name: string;
}
```

### 2.3. Сообщение
```
Message: {
    message: string;
    author: string;
    created: string;
}
```


## 3. Список запросов
| Название | О чем |
| - | - |
| login | Авторизация пользователя |
| logout | Логаут пользователя |
| registration | Регистрация пользователя |
| changeName | Изменить имя пользователя | 
| changePassword | Изменить пароль пользователя |
| sendMessage | Отправить сообщение в чат |
| getMessages | Получить сообщения в чате |
| getSpline | Получение сплайновой апроксимации для отрисовки карты игры |
| generateRandomPoints | Генерация случайных точек в заданном диапазоне |
| generateMap | Генерация карты с использованием случайных точек и сплайновой апроксимации |
| getCatalog | Получение каталога игрового магазина |
| getPosition | Получение позиции лемминга |
| givePosition | Запись позиции лемминга |
| changeClass | Смена класса лемминга |
| changeItem | Смена предмета лемминга |
| telepotLemming | Телепорт лемминга в рандомную точку |
| adminLogoutUser | Возможность администратору сбросить хэш пользователя по логину |
| buyItem | Покупка предметов в магазине и помощение их в инвентарь |


### 3.1. Общие ошибки
* `101` - если не передан параметр `method`
* `102` - если переданное значение в параметре `method` не обрабатывается
* `242` - не переданы все необходимые параметры


## 4. Подробно
### 4.1. login
Авторизация пользователя в системе

**Параметры**
```
{
    login: string; - логин пользователя
    hash: string; - контрольная сумма, равная hash = md5(nd5(login + password) + rnd)
    rnd: number; - случайно целое число
}
```
**Успешный ответ**
```
    Answer<User>
```
**Ошибки**
* `1002` - неверный пароль
* `1005` - неверный логин


### 4.2. logout
Вылогин пользователя из системы

**Параметры**
```
{
    token: string
}
```
**Успешный ответ**
```
    Answer<true>
```
**Ошибки**
* `1003` - неверный пароль


### 4.3. registration
Зарегистрировать нового ползователя

**Параметры**
```
{
    login: string; - логин пользователя
    password: string; - хеш пароля: md5(login + password)
    name: string; - имя пользователя
}
```
**Успешный ответ**
```
    Answer<User>
```
**Ошибки**
* `1001` - такой логин уже существует
* `1004` - ошибка при регистрации пользователя


### 4.4. sendMessage
Послать сообщение в чат

**Параметры**
```
{
    token: string; - токен
    message: string; - текст сообщения
}
```
**Успешный ответ**
```
    Answer<true>
```
**Ошибки**
* `705` - невалидный токен. Пользователь не авторизован


### 4.5. getMessages
Получить все сообщения чата

**Параметры**
```
{
    token: string; - токен
    hash: string; - хеш-сумма чата
}
```
**Успешный ответ**
```
    Answer<{
        messages: Message[]; - список сообщений
        hash: string; - новый хеш чата
    }>
```
**Примечание**: в случае отсутствия новых сообщений будет ответ:
```
    Answer<
        hash: string; - новый хеш чата
    >
```
**Ошибки**
* `705` - невалидный токен. Пользователь не авторизован


### 4.6. getSpline
Метод для расчёта коэффициентов кубического сплайна на основе заданного набора точек.

**Параметры**
```
$points: array - массив точек, каждая точка представлена в виде ассоциативного массива с ключами 'x' и 'y', например:
[
    ['x' => 1, 'y' => 2],
    ['x' => 2, 'y' => 3],
    ...
]
```
**Успешный ответ**
```
    [
    'a' => array, // коэффициенты a (значения y)
    'b' => array, // коэффициенты b
    'c' => array, // коэффициенты c
    'd' => array, // коэффициенты d
    'h' => array  // разности по x между соседними точками
]
```
**Описание**
Метод использует алгоритм расчёта коэффициентов кубического сплайна для интерполяции заданного набора точек. На основе входного массива точек вычисляются массивы коэффициентов a, b, c, d, и промежуточные значения h, необходимые для дальнейших вычислений интерполируемых значений.
**Ошибки**
* `4001` - передан некорректный массив точек 
* `4002` - недостаточное количество точек для построения сплайна


### 4.7. generateRandomPoints
Генерирует случайные точки в заданном диапазоне.

**Параметры**
```
{
    numPoints: number; - количество точек
    xMin: number; - минимальное значение для координаты x
    xMax: number; - максимальное значение для координаты x
    yMin: number; - минимальное значение для координаты y
    yMax: number; - максимальное значение для координаты y
}
```
**Успешный ответ**
```
    Answer<{
        points: { x: number; y: number; }[]; - массив точек
    }>
```
**Ошибки**
* `242` - не переданы все необходимые параметры


### 4.8. generateMap
Создает карту с использованием случайных точек и сплайн-аппроксимации.

**Параметры**
```
{

}
```
**Успешный ответ**
```
    Answer<{
        coeffs: number[];
        points: { x: number; y: number; }[];
    }>
```
**Ошибки**
* `4001` - передан некорректный массив точек


### 4.9. changeName
Изменение имени пользователя. 

**Параметры**
```
{
    token: string; - токен 
    name: string; - новое имя пользователя
}
```
**Успешный ответ**
```
    Answer<{
        name: string; - новое имя пользователя
    }>
```
**Ошибки**
* `705` - невалидный токен. Пользователь не авторизован
* `242` - не переданы все необходимые параметры


### 5.0. changePassword
Изменение пароля пользователя. 

**Параметры**
```
{
    token: string; - токен 
    oldPassword: string; - старый пароль
    newPassword: string; - новый пароль 
}
```
**Успешный ответ**
```
    Answer<true>
```
**Ошибки**
* `705` - невалидный токен. Пользователь не авторизован
* `1002` - неверный старый пароль
* `242` - не переданы все необходимые параметры


### 5.1. getCatalog
Метод возвращает массив товаров, каждый из которых представлен объектом с уникальным идентификатором, названием и ценой. Этот метод не требует входных параметров.

**Успешный ответ**
```
    Answer<{
        catalog: {
            id: number; - уникальный идентификатор товара
            name: string; - название товара
            price: number; - стоимость товара
        }[];
    }>
```


## 5.2. respawnObjects
Метод для обновления объектов на карте. Удаляет "старые" объекты (например, отмеченные как удалённые) и добавляет новые.

**Параметры**
```
{
    existingObjects: { x: number; y: number; removed: boolean; }[];  - массив текущих объектов на карте, где каждый объект представлен координатами `x`, `y` и полем `removed`, указывающим на необходимость удаления объекта.

    numNewObjects: number; - количество новых объектов, которые нужно сгенерировать.

    xMin: number; - минимальное значение для координаты `x` новых объектов.

    xMax: number; - максимальное значение для координаты `x` новых объектов.

    yMin: number; - минимальное значение для координаты `y` новых объектов.

    yMax: number; - максимальное значение для координаты `y` новых объектов.
}
```

**Успешный ответ**
```
Answer<{
    objects: { x: number; y: number; }[];
}>
```
**Ошибки**
* `242` - не переданы все необходимые параметры.
* `4003` - некорректный массив существующих объектов.
* `4004` - параметры для генерации новых объектов находятся вне допустимого диапазона.


## 5.3. givePosition

Метод для обновления позиции объекта в таблице user_lemming. Если для пользователя уже существует запись, она будет удалена и заменена новой.

**Параметры**
```
    {
    user_id: string; - идентификатор пользователя
    lemming_id: string; - идентификатор объекта (лемминга)
    x: number; - координата x объекта
    y: number; - координата y объекта
    direction: string; - направление движения
    status: string; - текущее состояние объекта
    }
```

**Успешный ответ**
```
    Answer<{true}>
```

**Ошибки**
* `705` - невалидный токен. Пользователь не авторизован
* `242` - не переданы все необходимые параметры


## 5.4. getPosition

Метод для получения позиции объекта по user_id.

**Параметры**
```
    {
    user_id: string; - идентификатор пользователя
    }
```

**Успешный ответ**
```
    Answer<{
        user_id: string;
        lemming_id: string;
        x: number;
        y: number;
        direction: string;
        status: string;
    }>
```

**Ошибки**
* `705` - невалидный токен. Пользователь не авторизован
* `242` - не переданы все необходимые параметры


### 5.5 getScores
Получение поинтов за прохождение уровня, сбор монет и подсчёт количества смертей

**Параметры**
```
    token: string; - токен пользователя
    action: string; - действие, которое необходимо выполнить (addGold, addLevel, addDeath)
```
**Успешный ответ**
```
Answer<{
    points: integer - новое количество очков
    death: integer - новое количество смертей
}>
```
**Ошибки**
* `705` - невалидный токен. Пользователь не авторизован
* `242` - не переданы все необходимые параметры
* `710` - действие не найдено


### 5.6 changeClass
Менет класс лемминга у пользователя

**Параметры**
```
    {
    token: string; - токен пользователя
    lemming_id: string; - идентификатор объекта (лемминга)
    newClass: string; - Новый класс
    }
```
**Успешный ответ**
```
    Answer<true>
```
**Ошибки**
* `705` - невалидный токен. Пользователь не авторизован
* `242` - не переданы все необходимые параметры


### 5.7 changeItem
Меняет прошлую карточку на новую

**Параметры**
```
    {
    token: string; - токен пользователя
    user_id: string; - идентификатор пользователя
    oldTypeId: string; - старый предмет
    newTypeId: string; - новый предмет
    }
```
**Успешный ответ**
```
    Answer<true>
```
**Ошибки**
* `705` - невалидный токен. Пользователь не авторизован
* `242` - не переданы все необходимые параметры


### 5.8 telepotLemming
Телепортирует игрока в рандомную точку

**Параметры**
```
    {
    token: string; - токен пользователя
    lemming_id: string; - идентификатор объекта (лемминга)
    }
```
**Успешный ответ**
```
    Answer<true>
    newPosition: { x: number; y: number; }[];
```
**Ошибки**
* `705` - невалидный токен. Пользователь не авторизован
* `242` - не переданы все необходимые параметры


### 5.9 adminLogoutUser
Возможность администратору сбросить хэш пользователя по логину

**Параметры**
```
    {
    token: string; - токен пользователя
    login: string; - логин пользователя
    }
```
**Успешный ответ**
```
    Answer<true>
```
**Ошибки**
* `720` - пользователь не администратор
* `705` - невалидный токен. Пользователь не авторизован
* `242` - не переданы все необходимые параметры


### 5.10 buyItem
Покупка предметов в магазине и помощение их в инвентарь

**Параметры**
```
    {
    token: string; - токен пользователя
    item_id: integer; - айди предмета
    }
```
**Успешный ответ**
```
    Answer<true>
```
**Ошибки**
* `721` - предмет не найден
* `711` - очки не найдены
* `705` - невалидный токен. Пользователь не авторизован
* `242` - не переданы все необходимые параметры